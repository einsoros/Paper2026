---
last-updated: 2026-08-17
type: handoff
project: Paper2026
status: 단일 파일로 운용 — 스레드 전환 때마다 이 파일을 덮어쓴다. 이력은 git이 갖는다
---

# HANDOFF — Paper2026

> **이 파일은 하나만 유지한다.** 스레드가 바뀔 때마다 덮어쓴다. 날짜별 HANDOFF를 새로 만들지 않는다.
> **작업 내용은 여기 없다.** 무엇을 할지는 `Analysis/action-items-260817.md` 하나에 전부 있다.

---

## 1. 기본 정보

| | |
|---|---|
| 리포 | `github.com/einsoros/Paper2026` |
| 최신 커밋 | **`cb29b6f`** (review 6편 archive 이동) ⚠️ 경로 문자열 12곳이 깨진 상태 — §9 |
| 작업 기준 파일 | **`Analysis/action-items-260817.md`** ← 이거 하나만 읽으면 된다 |
| 심사 | **8월 말 프로포절 — 약 2주** · 지도교수 면담 **일정 미정** |
| 최대 마감 위험 | 🔴 **발표자료 실물 0장** |

⚠️ 원격 파일 확인은 **반드시 커밋 SHA를 경로에 넣을 것.** `main` 경로는 CDN 캐시로 구본을 반환한 적이 있다.

---

## 2. 연구 개요

**주제** — 인간-AI 협업 환경에서 성과 기여 귀속 모호성이 절차공정성 지각에 미치는 영향

```
IV  성과 기여 귀속 모호성   시나리오 2조건 (검토·수정의 밀도)
 ↓
MV  지각된 평가 불확실성    4문항  ⚠️ 측정모형 형식 미확정
 ↓
DV  절차공정성 지각         3문항  ✅ 08-17 확정, 신규 0
MOD 평가 절차 명확성        ⚠️ 문항 재설계 필요 (H4 존치)
```

**이론 4단** — Weiner(1985) 귀인이론 기대원리 → Van den Bos(2001) UMT → Leventhal(1980)·Colquitt(2001) 절차공정성 / MV 측정 뿌리는 Rizzo et al.(1970) 정의 (1)

**핵심 문제의식** — 평가 시스템의 결함이 아니라 **산출물에서 기여가 분리되기 어려운 성질에 기인한 인식 조건의 문제**이며, 따라서 절차 개선만으로는 원인에 닿지 못한다.

---

> **작업 방침** — 미결을 면담까지 남겨두지 않는다. 모든 요소를 최대한 완성한 뒤, 그 지점에서 **남은 의문과 위험요소를 안건으로 정리해** 지도를 받는다.

---

## 3. ★ 연구 태도 — 이게 없으면 같은 사고가 반복된다

| # | |
|---|---|
| 1 | **판단하지 않고 확인한다.** 원문을 보기 전 추론을 확정처럼 말하지 않는다. **확인한 것 / 추론한 것 / 미확인**을 구분한다 |
| 2 | **근거 위치를 파일 → 절 제목 → 소제목/표 이름으로 밝힌다.** ⚠️ **행 번호를 쓰지 않는다** — 성준님이 Obsidian에서 셀 수 없고 편집하면 어긋난다. 행 번호는 **파일 전달 시 `diff --stat` 예측에만** 쓴다. 출처 없는 서술은 검증 불가다 |
| 3 | **과잉 주장 금지.** 「첫 시도」·「최초」·「아무도」 금지. 확보한 문헌 수를 넘는 일반화를 하지 않는다 |
| 4 | **「측정되지 않은」** — 「못한」·「누락」·「편향」 금지. 원전에 귀책을 두지 않는다. 원전 저자의 의도를 추정하지 않는다 |
| 5 | **문항·정의문은 성준님이 쓴다.** Claude는 **구조와 예문**을 먼저 제시한다 |
| 6 | **실무 기여를 배제하지 않는다.** 중심에 두지 않을 뿐이다 |
| 7 | **편들지 않는다.** 근거로만 말한다. 약어는 처음 나올 때 풀어서 병기한다 |

### 반복된 판단 오류 패턴

| 패턴 | 사례 |
|---|---|
| **리포 기록을 실물보다 우선** | 낡은 `paper-feature-matrix`를 믿고 08-16에 3회 오판. Colquitt 원문항이 `Sources/Papers/`에 이미 있었는데 못 찾음 |
| **원문 확인 전 추론을 확정처럼** | 08-17 CFA 정확식별·1인칭 시점 고정 두 논거를 근거로 제시했다가 검증 후 철회 |
| **어휘 함의 오독** | 「측정되지 못한」 계열 |

---

## 4. ★ 파일 전달·커밋 프로세스

| # | |
|---|---|
| 1 | **파일 배치는 성준님이 직접 한다.** Claude는 **파일 + 리포 경로 표 + 예상 `diff --stat` + git 명령(`add`·`commit`·`push`)** 만 제공한다. `copy`·`mv` 명령은 주지 않는다 |
| 2 | **리포에 들어갈 파일만** 전달한다. 커밋 메시지는 채팅에만 쓴다 |
| 3 | 배치 후 **`git --no-pager diff --stat`으로 예상 줄 수 확인.** 08-15에 같은 커밋이 두 번 올라가 A0-2 작업이 유실된 사례 |
| 4 | **두 기기에서 같은 배치를 하지 않는다.** 한 기기에서만 커밋하고 다른 기기는 `pull`로 받는다 (08-17 중복 커밋 사례) |
| 5 | 시작 = `git pull` / 종료 = 반드시 `push` (미완이어도) |
| 6 | ⚠️ **frontmatter 값에 콜론을 넣지 않는다.** YAML 파싱이 깨진다 |
| 7 | ⚠️ 리포 폴더는 **`Zotero/`(대문자)** — macOS는 대소문자를 구분하지 않아 `mp`에서는 열리지만 `wp`에서 깨진다 |

### 기기

| | 커밋 접두 |
|---|---|
| `wp` (Windows, `C:\Users\einso\Desktop\Research\Paper2026`) | `[wp]` |
| `mp` (Mac) | `[mp]` |

⚠️ **Zotero 무료 계정이라 PDF 첨부는 기기 간 동기화되지 않는다.** 서지만 넘어온다.

---

## 5. ★ action-items 운용 규칙 [08-17 신설]

| 종류 | 성격 | 규칙 |
|---|---|---|
| `action-items-*` | **휘발성** | 새 파일은 **자립**한다. 미결은 링크가 아니라 **본문 이관**. 이전 파일은 종료 표기 후 아카이브 |
| `review-*` | **시점 기록** | 누적. 수정하지 않음 |
| `progress/HANDOFF.md` | **단일 파일** | 덮어쓴다. 날짜별로 늘리지 않는다 |
| `Analysis/`·`Writing/` | **영속** | 결론이 사는 곳 |

> **결론은 영속 파일로, 미결은 최신 action-items로.** action-items끼리 서로 참조해 사슬이 되면 안 된다.

---

## 6. 08-17 작업 — 커밋 5건

| SHA | 내용 |
|---|---|
| `c0de757` | Moorman(1991)·Greenberg(1986)·Leventhal(1980) 원문 노트 3편 + action-items-260817 |
| `b374c3c` | **DV2 폐기 확정** — measurement-items §III V20 · logic-architecture · action-items 2편 |
| `9bf6ba3` | 후속 반영 — Literature_Map · paper-feature-matrix V19 · 01-Dashboard |
| `15d5cda` | **action-items-260817 자립화** — 미결 전량 이관 |
| `fd080ec` | **A0-3 완료** — proposal-draft §03 학문적 기여 5개 (v1.7) |

**닫은 것** — DV2 판정 · action-items 사슬 구조 · A0-3

---

## 7. ✅ 종결 — I1 (MV 측정모형 형식) [08-17]

**판정** — **단일차원 · reflective 확정. 문항 4개 유지. 연쇄 변경 없음.**

결론이 사는 곳은 **`Analysis/scale-development_MV.md` §10 「측정모형 형식 판정」**이다. 아래는 요약일 뿐이므로 실제 작업 시 §10을 연다.

| | |
|---|---|
| 차원성 | 단일차원 — D1·D2는 차원이 아니라 **내용 영역 구분** (§10-2) |
| 형식 | reflective — Figure 3 Panel A (§10-4) |
| 적용 지표 | α ≥ .70 · AVE > .50 · 표준화 적재량² > .50 · AVE > 상관² |
| MIMIC | 불필요. 문항 4→6 시나리오 소멸 |

### ⚠️ 이 판정에 대한 우려 5건 — `scale-development_MV.md` §10-5

⚠️ **면담 일정이 미정이므로 「면담 전」으로 적지 않았다.** 각 우려에 **발동 지점**(어느 작업을 열면 함께 처리되는지)이 붙어 있다.

| 우려 | 발동 지점 |
|---|---|
| 1. **Q3 근거가 이론이 아니라 설계 의도** | **§4 G1 3단 논거 ③** |
| 2. 판별타당도 3쌍 | `analysis-plan` 파일럿 설계 |
| 3. **CDAA Boundary 순환** | **§4 착수 조건** |
| 4. **확신 미확보 상태 채택** | **§7 면담 안건 4번 작성 시** |
| 5. 판별 목록 인용 제약 | 발표자료·논문 서술 |

**재검토 발동 조건** — 파일럿 CFA에서 ① 적재량² < .50 ② α < .70 ③ D1·D2가 별개 요인으로 분리 — 셋 중 하나면 차원성 판정부터 다시 연다.

### 🔴 파생 미결 — CDAA §II Boundary Sentence

`Analysis/construct-definition_attribution-ambiguity.md` §II → Boundary Sentence가 *"…기여를 귀속할 수 없는 상태에서 비롯되는…"*으로 **IV를 MV 정의문에 포함**한다. MacKenzie p.300 Table 1 위반이며, 같은 파일 §I·§II의 「순환 방어 최상단」과 충돌한다.

→ 정정 방향은 원인을 빼고 **두 참조점**(이미 산출된 성과 / 향후 받게 될 평가)만 남기는 것. **문장은 성준님이 작성.** §4 G1 방어 작업 때 함께 처리한다.

---

## 8. 그다음 순서

```
1. I1 MV 측정모형 형식        §3  ✅ 08-17 종결
2. A — G1 3단 논거            §4  ★★ ← 다음 작업. 면담 최대 쟁점. 재료는 있고 배열만
                                    CDAA Boundary Sentence 정정을 여기서 함께
3. 사전 검정력 대략치          §5  ★★ 면담 안건 근거
4. A2 — MOD 문항 재설계        §6  ★ 문항 조달 문헌 2편 필요
5. C 면담 안건 / B 발표자료     §7  🔴 실물 0장
6. D·E 설문 구성·파일럿        프로포절 후
```

---

## 9. ⬜ 잔여 — 급하지 않음

- [x] ~~**Zotero 키 대조**~~ ✅ **[08-17] 확인 완료** — `citation-keys.md` 65·66행에 `mackenzieConstructMeasurementValidation2011`·`heggestadScaleAdaptationOrganizational2019` 둘 다 「✅ 확보」로 등재됨
- [ ] **Bollen & Lennox(1991) · Jarvis et al.(2003) · Wong et al.(2008)** — 묶음 B 3개 질문의 원출처(p.301 명시). 미확보·미등재. 논문에서 3개 질문을 인용하려면 필요
- [ ] `Literature_Map` 변경 이력에 `[08-17]` 한 줄 (본문은 이미 반영됨)
- [ ] **Leventhal 출판본 쪽수** — 보유본은 ERIC 판본. 논문 인용 전 필수
- [ ] 🔴 **Weiner 서지** — 키에 연도 없음 + 간행 공란. `(Weiner, n.d.)`로 나올 위험
- [ ] 🔴 **경로 문자열 12곳 정정** — `cb29b6f`에서 `review-*` 6편을 `progress/archive/`로 옮겼으나 선행 정정을 하지 않아 깨진 상태
  - `Analysis/` 6개 파일 frontmatter `related-review` + 본문 3곳 → `paper-feature-matrix`·`construct-definition_attribution-ambiguity`·`analysis-plan`·`logic-architecture`·`measurement-items`·`research-model`
  - `progress/context-summary.md` frontmatter · `01-Dashboard.md` 이력 표 2곳
  - ✅ `00-Hub.md`와 `review-*` 내부는 `[[위키링크]]`·과거 기록이라 손대지 않는다
- [ ] **파일 정리 잔여** — `action-items` · `review` 누적
  - 순서: ① 경로 문자열 12곳 → 위키링크 정규화 ② `00-Hub` 재작성(현재 `260814`를 「현행」으로 가리킴) ③ `git mv` 아카이브
  - ⚠️ Obsidian `[[위키링크]]`는 하위 폴더로 옮겨도 안 깨진다. **경로 문자열만** 깨진다
  - ⚠️ `review-2026-06-15`는 V12~V16 설계 근거라 삭제하지 말 것

---

## ▶ 새 스레드 시작 메시지

```
고려대 기술경영전문대학원 석사논문 Paper2026 진행 중입니다.
리포는 github.com/einsoros/Paper2026, 최신 커밋은 fd080ec입니다.

★ 먼저 progress/HANDOFF.md를 읽어주세요. §3 연구 태도와 §4 파일 프로세스가
없으면 같은 사고가 반복됩니다.
작업 내용은 Analysis/action-items-260817.md 하나에 전부 있습니다.
이전 action-items는 열 필요 없습니다.

지금 작업은 I1 — MV 측정모형 형식 확정입니다.
MacKenzie et al.(2011) PDF를 올릴 테니 Step 4 Construct Dimensionality 절의
판단 질문 3개를 뽑아 measurement-items §II의 MV 4문항과 대조해 주세요.
판정 기준은 HANDOFF §7에 미리 고정해 두었습니다.

지켜주실 것:
- 근거 위치를 파일·절 단위로 밝혀주세요.
  확인한 것 / 추론한 것 / 미확인을 구분해 주세요.
- 문항과 정의문은 제가 직접 씁니다. 구조와 예문을 먼저 주세요.
- 제 편을 들지 말고 근거로만 말해주세요.

파일을 주실 때는 리포 경로와 예상 diff --stat, git 명령만 주세요.
배치는 제가 직접 합니다. copy 명령은 주지 마세요.
커밋 접두는 [wp]이고, 커밋 메시지는 채팅에만 써주세요.

원격 파일 확인은 반드시 커밋 SHA를 경로에 넣어주세요.
main 경로는 CDN 캐시 때문에 구본을 반환한 적이 있습니다.
```
