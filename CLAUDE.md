# Paper2026 — 작업 지침

이 리포는 석사학위논문(Paper2026) 작업공간이다.
**현재 활성 트랙은 `PlanW/` 하나뿐이다.**

## 0. 새 스레드가 가장 먼저 할 일

1. `PlanW/99-HANDOFF.md` 를 읽는다. 현재 상태의 압축본이다.
2. `PlanW/00-PLANW.md` 를 읽는다. 운영원칙 전체가 여기 있다.
3. 그 다음 `PlanW/01-RESEARCH-MODEL.md`, `PlanW/06-PROGRESS.md`.

**이 지침과 `PlanW/00-PLANW.md` 가 충돌하면 `PlanW/00-PLANW.md` 가 우선한다.**

## 1. 읽지 않는 것

- `Archive/` 아래의 모든 것. `0-PlanA`, `1-PlanB`, `2-PlanC` 는 폐기된 트랙이며
  보존 목적으로만 남아 있다. **읽거나 복원하거나 상속하지 않는다.**
- `Archive/` 의 내용을 근거로 현재 방향을 추론하지 않는다.
- 사용자가 특정 파일을 명시적으로 지정한 경우에만 예외.

## 2. PlanW 운영원칙 요약

전문은 `PlanW/00-PLANW.md`. 핵심만 옮기면:

**근거등급을 반드시 구분한다.**

| 등급 | 의미 |
|---|---|
| A | 원전에서 저자가 직접 주장/검증한 것 |
| B | bibliographic / metadata로만 확인한 것 |
| C | Claude의 해석 / 추론 |
| D | 아직 검증되지 않은 가설 / 아이디어 |

C/D를 A처럼 표현하지 않는다.

**작업 순서를 건너뛰지 않는다.**
원전 확보 → 원전 정독 → construct·이론관계 확인 → 측정/척도 계보 → 후속연구 계보 →
backbone 안정성 판단 → (필요시) 최소 확장 → RQ/가설/모형 → 연구설계/측정계획.
뒤 단계 아이디어를 앞 단계로 끌어오지 않는다.

**금지**
- 새 변수·mediator·moderator·contribution을 원전 확인 전에 발명하지 않는다.
- 검색 부재를 research gap으로, 비유의성을 관계 부재로 판단하지 않는다.
- 측정대상·분석수준·평가주체·연구맥락을 임의로 바꾸지 않는다.
- 원전에 없는 bridge를 생성하지 않는다.
- 현 단계에서 AI / GenAI / automation / digitalization / algorithmic management /
  Human-AI collaboration 을 연구모형에 연결하지 않는다.
- 사용자의 현업 사례를 backbone 정당화 근거로 사용하지 않는다.

## 3. 파일 관리

**`새로운 연구단계 = 새로운 파일` 이 아니다.**

`PlanW/` 의 고정 문서 8개만 사용한다.

| 변경 유형 | 수정 대상 |
|---|---|
| 연구모형 변경 | `PlanW/01-RESEARCH-MODEL.md` |
| 이론 근거 추가 | `PlanW/02-THEORY.md` |
| 논문 추가 / 판정 | `PlanW/03-LITERATURE.md` |
| 척도 확인 | `PlanW/04-MEASUREMENT.md` |
| 중요 결정 | `PlanW/05-DECISIONS.md` |
| 진도 변경 | `PlanW/06-PROGRESS.md` |
| 인계 상태 변경 | `PlanW/99-HANDOFF.md` |

- 새 `.md` 생성은 원칙적으로 금지. 사용자와 협의 후에만.
- **파일명에 날짜를 넣지 않는다.** `NN-NAME.md` 형식만 사용한다.
- 날짜별 action-item, 단계별 결과, 후보별 파일, 매번의 review 파일을 만들지 않는다.
- 과거 상태는 stage 파일이 아니라 **Git history** 로 보존한다.

**문서 헤더** — 8개 문서 전부 YAML frontmatter 를 유지한다.

```yaml
---
문서: 03-LITERATURE
역할: 선행연구 대장 — 역할 · 원전 확인 여부 · 채택/보류/탈락
갱신: 2026-09-13 07:52 KST
핵심: 등록 1편 · 원전 확인 0편 · 채택 0편
---
```

`갱신` 은 **연구상태가 실질적으로 변경된 마지막 시점**이다. 오탈자·서식·표현 수정으로는
갱신하지 않는다. 그런 이력은 Git commit 이 보관한다.

## 4. 자료관리 3층

| 층위 | 도구 | Source of Truth |
|---|---|---|
| 서지정보 · 원전 | Zotero | 서지정보, DOI, 원문 PDF, metadata, collection |
| 연구논리 · 현재 상태 | `PlanW/` 8문서 | 현재의 진실 |
| 변경이력 · 과거 상태 | Git | 커밋 히스토리 |

PDF 확보와 원전 내용 검증은 구분한다. PDF 가 Zotero 에 있다는 사실은 B등급이지 A등급이 아니다.

## 5. Mission 종료 규칙

각 Mission 종료 시 다음을 보고한다.

1. 원전으로 확인된 것
2. 미확정인 것
3. 연구구조의 변화 여부
4. 다음에 확인할 작업

그리고 해당 문서를 갱신하고 `06-PROGRESS.md` · `99-HANDOFF.md` 를 현재 상태와 일치시킨다.

**다음 Mission 을 임의로 시작하지 않는다.** 보고하고 사용자의 지시를 기다린다.

## 6. 공통 금지

- 인용, DOI, 저자, 게재지, 결과를 지어내지 않는다.
- 근거가 약하거나 없으면 매끄럽게 넘기지 말고 그 사실을 적는다.
- 사용자가 지시하지 않은 git 명령을 실행하지 않는다.
- 사용자가 지시하지 않은 파일 산출을 하지 않는다. 판정·결과는 대화로 내고,
  문서화 시점은 사용자가 정한다.
